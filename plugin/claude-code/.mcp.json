{
  "mcpServers": {
    "process-os": {
      "command": "node",
      "args": ["--import", "tsx/esm", "$CLAUDE_PLUGIN_ROOT/src/mcp/server.ts"],
      "env": {
        "PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}",
        "CONTROL_PLANE_URL": "https://pos.yoda.digital"
      }
    }
  }
}
